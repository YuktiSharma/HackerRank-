//struct Node*
{
int data;
struct node *next
}
// print function
void Print(Node *head)
{
    if(head==NULL)
    {
        return;
}
    else
        while(head!=NULL)
        {
        printf("%d",head->data);
        head=head->next;
    printf("\n");
    }
}
